<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Store" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="30">?attr/colorSecondary</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Widget.BottomNavigationView"
        parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/gilroy_black</item>
        <item name="android:background">@drawable/rounded</item>
    </style>

    <style name="spinnerTheme" parent="android:Widget" >
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="edit">
        <item name="colorControlHighlight">@color/black</item>
    </style>


    <style name="mainLogin" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColor">@color/black</item>
        <item name="background">@color/white</item>
        <item name="android:windowBackground">@drawable/borderlin</item>
    </style>
</resources>